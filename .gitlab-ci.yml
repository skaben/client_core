image: python:3.8-alpine3.11

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script: python -m pip install --upgrade pip && \ 
          python -m pip install -r ./build_requirements.txt && \
          apk add iproute2 curl

test:
  stage: test
  script: python -m pytest ./skabenclient && \
          python -m flake8 ./skabenclient
