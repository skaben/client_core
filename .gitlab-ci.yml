stages:
  - build

build:
  stage: build
  image: python:3.8-slim-buster
  script:
    - apt-get update
    - apt-get install -y --no-install-recommends libglib2.0-0 iproute2 curl python3-pygame
    - python -m pip install --upgrade pip
    - python -m pip install -r ./build_requirements.txt
